# 🚀 Render Production Setup

## ✅ Что уже сделано:
- ✅ Backend код обновлен для поддержки DATABASE_URL
- ✅ Код запушен в GitHub
- ✅ Render автоматически начнет редеплой

## 📋 Шаги для завершения настройки:

### 1️⃣ **Добавить DATABASE_URL в Environment Variables**

1. Откройте https://dashboard.render.com/web/racket-pong-api
2. Перейдите в **Environment** (левое меню)
3. **Удалите** старые переменные:
   - `DB_HOST`
   - `DB_PORT`
   - `DB_NAME`
   - `DB_USER`
   - `DB_PASSWORD`
4. **Добавьте новую переменную**:
   - **Key**: `DATABASE_URL`
   - **Value**: `postgresql://table_tennis_tournament_user:gFhhIyDmTpIPrTCgSf6JrfhyepUhqZiD@dpg-d4gh21fdiees73at4srg-a/table_tennis_tournament`
5. **Save Changes**

### 2️⃣ **Настроить базу данных (один раз)**

После того как backend перезапустится (шаг 1), выполните:

1. Откройте **Shell** в Render (racket-pong-api → Shell)
2. Выполните команду:
   ```bash
   node scripts/setup-production-db.js
   ```

Это создаст все таблицы и структуру базы данных.

### 3️⃣ **Готово!**

После выполнения этих шагов:
- ✅ Backend подключится к облачной БД
- ✅ Все таблицы будут созданы
- ✅ Сайт начнет работать

---

## 🔧 Если что-то пошло не так:

### Проверить логи:
```
https://dashboard.render.com/web/racket-pong-api/logs
```

### Проверить подключение к БД:
В Shell выполните:
```bash
echo $DATABASE_URL
```

### Вручную создать таблицы:
Если автоматическая миграция не сработает, можно подключиться к БД через psql:
```bash
psql postgresql://table_tennis_tournament_user:gFhhIyDmTpIPrTCgSf6JrfhyepUhqZiD@dpg-d4gh21fdiees73at4srg-a/table_tennis_tournament
```

И выполнить SQL файлы из папки `backend/database/`.
