# Как запустить бэкенд для просмотра логов в реальном времени

## Способ 1: Запуск в текущем терминале (рекомендуется)

Откройте **новый терминал** и выполните:

```bash
cd "/Users/leonidtkach/Table Tennis Tournament Website 3/backend"
npm run server
```

**Важно:** 
- Терминал будет занят выводом логов
- Для остановки нажмите `Ctrl+C`
- Все логи будут отображаться в реальном времени

## Способ 2: Запуск с сохранением логов в файл

Если хотите одновременно видеть логи и сохранять их:

```bash
cd "/Users/leonidtkach/Table Tennis Tournament Website 3/backend"
npm run server 2>&1 | tee logs/backend.log
```

Затем в другом терминале можно просматривать логи:
```bash
tail -f backend/logs/backend.log
```

## Способ 3: Запуск в фоне с просмотром логов

1. Запустите бэкенд в фоне:
```bash
cd "/Users/leonidtkach/Table Tennis Tournament Website 3/backend"
npm run server > logs/backend.log 2>&1 &
```

2. Просматривайте логи в реальном времени:
```bash
tail -f backend/logs/backend.log
```

3. Остановите бэкенд:
```bash
lsof -ti:3003 | xargs kill
```

## Что вы увидите в логах:

### При запуске:
```
🚀 Server running on port 3003
📊 API available at http://localhost:3003/api
Connected to PostgreSQL database
```

### При изменении результата матча:
```
[🏆 UPDATE WINNER] ========================================
[🏆 UPDATE WINNER] Winner changed in match 285 (Quarterfinals)
[🏆 UPDATE WINNER] Old winner: 6
[🏆 UPDATE WINNER] New winner: 7
[🔄 CASCADE RESET] Starting cascade reset...
[📊 UPDATE SCORES] Match ID: 285
...
```

## Фильтрация важных логов

Чтобы видеть только важные логи (обновления матчей):

```bash
cd "/Users/leonidtkach/Table Tennis Tournament Website 3/backend"
npm run server 2>&1 | grep -E "\[🏆|\[🔄|\[📊|ERROR|WARN"
```

## Проверка, что бэкенд запущен

Откройте другой терминал и проверьте:

```bash
curl http://localhost:3003/api/health
```

Должен вернуться: `{"status":"OK","message":"Table Tennis Tournament API is running"}`

## Остановка бэкенда

Нажмите `Ctrl+C` в терминале, где запущен бэкенд, или:

```bash
lsof -ti:3003 | xargs kill
```

## Рекомендации

1. **Используйте отдельный терминал** для запуска бэкенда, чтобы видеть логи
2. **Не закрывайте терминал**, пока тестируете - логи будут теряться
3. **Сохраняйте логи в файл** при тестировании сложных сценариев
4. **Используйте фильтры** для поиска конкретных событий





