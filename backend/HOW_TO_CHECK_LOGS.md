# Как проверить логи бэкенда

## Запуск бэкенда с логированием

Бэкенд выводит все логи в консоль (терминал). Чтобы увидеть логи:

### 1. Запустите бэкенд в отдельном терминале

```bash
cd backend
npm run server
# или
node server/index.js
```

### 2. Логи будут отображаться в реальном времени

Все действия с матчами и турнирами логируются с префиксами:

- `[🏆 UPDATE WINNER]` - обновление победителя матча
- `[🔄 CASCADE RESET]` - каскадный сброс зависимых матчей
- `[📊 UPDATE SCORES]` - обновление счёта матча
- `[🏆 MATCH UPDATE]` - обновление матча
- `[🏆 ADVANCE]` - продвижение победителя в следующий раунд
- `[🔴 SINGLE-ELIMINATION]` - создание матчей олимпийской системы
- `[🔵 MATCH GENERATION]` - генерация матчей

## Важные логи для отладки проблемы с winner_id

Когда вы изменяете результат матча, следите за следующими логами:

### При изменении победителя матча:

```
[🏆 UPDATE WINNER] ========================================
[🏆 UPDATE WINNER] Winner changed in match 285 (Quarterfinals)
[🏆 UPDATE WINNER] Old winner: 6
[🏆 UPDATE WINNER] New winner: 7
```

### Проверка сброса зависимых матчей:

```
[🔄 CASCADE RESET] ========================================
[🔄 CASCADE RESET] Starting cascade reset for match 285 in Quarterfinals
[🔄 CASCADE RESET] Before reset - Match 289:
[🔄 CASCADE RESET]   - player1_id: 6
[🔄 CASCADE RESET]   - player2_id: 8
[🔄 CASCADE RESET]   - winner_id: 6  ← ДОЛЖЕН БЫТЬ ОЧИЩЕН
[🔄 CASCADE RESET] After reset - Match 289:
[🔄 CASCADE RESET]   - winner_id: NULL  ← ДОЛЖЕН БЫТЬ NULL
```

### Проверка обновления следующего раунда:

```
[🏆 UPDATE WINNER] Target match 289 in Semifinals:
[🏆 UPDATE WINNER]   - player1_id: 7  ← НОВЫЙ ПОБЕДИТЕЛЬ
[🏆 UPDATE WINNER]   - player2_id: 8
[🏆 UPDATE WINNER]   - winner_id: NULL  ← ДОЛЖЕН БЫТЬ NULL
[🏆 UPDATE WINNER] ✅ Set 7 in Semifinals Match 0 (player1)
[🏆 UPDATE WINNER]   - After update, winner_id = NULL (should be NULL)
```

### При обновлении счёта:

```
[📊 UPDATE SCORES] ========================================
[📊 UPDATE SCORES] Match ID: 285
[📊 UPDATE SCORES] Match completion check:
[📊 UPDATE SCORES]   - Old winner: 6
[📊 UPDATE SCORES]   - New winner: 7
[📊 UPDATE SCORES]   - Winner changed: true
[📊 UPDATE SCORES] ⚠️ Winner changed from 6 to 7, calling updateNextRoundOnWinnerChange
```

## Сохранение логов в файл

Если нужно сохранить логи в файл:

```bash
# Запуск с сохранением логов в файл
npm run server > logs/backend.log 2>&1

# Или с фильтрацией только важных логов
npm run server 2>&1 | grep -E "\[🏆|\[🔄|\[📊|ERROR"
```

## Просмотр логов в реальном времени

Если бэкенд уже запущен, можно использовать `tail` для просмотра логов:

```bash
# Если логи сохранены в файл
tail -f logs/backend.log | grep -E "\[🏆|\[🔄|\[📊"
```

## Проверка конкретного матча

Чтобы отследить обновление конкретного матча (например, матч ID 285):

```bash
npm run server 2>&1 | grep -E "285|Match 285"
```

## Частые проблемы и что искать в логах

### Проблема: winner_id не очищается

**Что искать:**
```
[🏆 UPDATE WINNER] ❌ ERROR: winner_id was NOT cleared! Value: 6
```

**Решение:** Проверьте, что функция `updateNextRoundOnWinnerChange` правильно очищает `winner_id`.

### Проблема: старый победитель остаётся в следующем раунде

**Что искать:**
```
[🏆 UPDATE WINNER] Current winner_id: 6  ← СТАРЫЙ ПОБЕДИТЕЛЬ
[🏆 UPDATE WINNER] Old winner: 6, New winner: 7
```

**Решение:** Проверьте, что `resetDependentMatches` вызывается ДО обновления следующего раунда.

### Проблема: дублирование обновлений

**Что искать:**
```
[🏆 UPDATE WINNER] Winner changed... (повторяется несколько раз)
```

**Решение:** Убедитесь, что фронтенд не вызывает API дважды или что нет гонки условий.

## Проверка базы данных напрямую

Также можно проверить состояние матчей напрямую в базе данных:

```sql
-- Проверить матчи турнира
SELECT id, round, player1_id, player2_id, winner_id, status 
FROM matches 
WHERE tournament_id = 4 
ORDER BY round, id;

-- Проверить конкретный матч
SELECT id, round, player1_id, player2_id, winner_id, status 
FROM matches 
WHERE id = 289;
```

## Рекомендации

1. **Всегда запускайте бэкенд в отдельном терминале**, чтобы видеть логи в реальном времени
2. **Обращайте внимание на префиксы логов** - они помогут быстро найти нужную информацию
3. **Проверяйте логи ПОСЛЕ каждого изменения** результата матча
4. **Сохраняйте логи в файл** при тестировании сложных сценариев
5. **Используйте фильтры** для поиска конкретных ошибок или событий





