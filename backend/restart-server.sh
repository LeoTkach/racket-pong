#!/bin/bash

# Скрипт для перезапуска бэкенд сервера

echo "🛑 Остановка сервера на порту 3003..."

# Найти и остановить процесс на порту 3003
PID=$(lsof -ti:3003)
if [ -n "$PID" ]; then
    echo "   Найден процесс с PID: $PID"
    kill -9 $PID 2>/dev/null
    sleep 1
    echo "   ✅ Процесс остановлен"
else
    echo "   ℹ️  Процесс не найден (порт свободен)"
fi

echo ""
echo "🚀 Запуск сервера..."
echo "📊 Логи будут отображаться ниже"
echo "⚠️  Для остановки нажмите Ctrl+C"
echo ""
echo "=========================================="
echo ""

cd "$(dirname "$0")"
node server/index.js

